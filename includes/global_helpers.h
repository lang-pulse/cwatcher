#include "common_includes.h"

#ifndef cwatcher_global_helpers_h
#define cwatcher_global_helpers_h

// Show error message and exit
void error(char*, int);

// Check if character is digit or not
bool is_digit(char);

// Check if character is alphabet or not
bool is_alpha(char);

// Check if character is alphabet/number or not
bool is_alnum(char);

#endif
